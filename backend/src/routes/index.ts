import { Router } from 'express';

const router = Router();

// TODO: Importare e montare le singole rotte qui
// import authRoutes from './auth.routes';
// router.use('/auth', authRoutes);

export default router;
